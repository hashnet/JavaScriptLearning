function Kid(name, candy) {
    this.name = name;
    this.candyandy = candy;
    this.nextKid = null;
}
Kid.prototy
Kid.prototype.count = function(sumSoFar) {
    setTimeout( 
        () => {
            if(!next) return error(this.name + 'has no nextKid set');
            sumSoFar += this.
            this.tellTo(nextKid);
        }, 2);
}
Kid.prototype.tellTo = funtion(sumSoFar, nextKid) {
    nextKid.count(sumSoFar);
}
Kid.proto


console.log("Creating kids.");
najat = new Kid('Najat', 1);
namir = new Kid('Namir', 2);
lilly = new Kid('Lilly', 3);


function najatCounts(other) {
    setTimeout(najat.count)
}
function namirCounts(other) {
    setTimeout()
}